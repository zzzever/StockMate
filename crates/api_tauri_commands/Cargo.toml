[package]
name = "api_tauri_commands"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
domain = { path = "../domain" }
storage = { path = "../storage" }
tokio = { version = "1", features = ["full"] }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
