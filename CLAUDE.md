## 自动使用 checklist-loop

凡是涉及 **bug 修复、功能新增、UI 改版、设计调整、数据源切换** 等非 trivial 任务，必须使用 `/checklist-loop` skill 流程：

1. **Round 0**: 分析问题 → 制定 checklist → 提交用户审批
2. **Round 1**: 用户确认后执行 → B review → C/D/E 测试 → 提交结果
3. **Round 2+**: 根据用户反馈修复 FAIL 项，最多 3 轮
4. 构建前后端

---

## ⚠️ 绝对强制规则：每次修改后必须构建前后端（不可跳过）

**每次修改代码后，必须立即构建前端和后端，缺一不可。不要等用户说"重新构建前后端"——主动构建。**

```bash
# Step 1: 前端构建
cd ui && npm run build

# Step 2: 后端构建
cd .. && "$HOME/.cargo/bin/cargo" build --release
```

**验证标准：**
- 前端 `npm run build` 必须零错误退出
- 后端 `cargo build --release` 必须零错误退出
- 构建失败 = 任务未完成，必须修复后重新构建
- 不允许"前端改了只构建前端"或"后端改了只构建后端"——每次都构建两边

- `target/release/stockmate-tauri.exe` 为最终可执行文件
- 构建完成后提醒用户重启 app

---

## 零容忍原则

**不管是本次修改引入的错误还是预先存在的错误，都必须修复。不能逃避任何问题。**

- 构建错误（前端+后端）：必须全部修复
- 测试失败（所有语言/框架）：必须全部修复，不管是不是本次修改引入的
- 类型错误（TypeScript/Rust）：必须清零
- 任何 warning/error：必须清零

每次修改后运行完整的构建和测试套件，确保零错误、零失败。

---

## 专业标准（强制执行）!!!!!!!!!!

**用户原话："好好设计！好好实现！好好测试！不错过任何一个细节，要尽善尽美，要非常专业，不要逃避责任，不要兜圈子，每个功能和测试落到实处。设计要是大牌专业风格。"**

**好好设计！好好实现！好好测试！不错过任何一个细节，要尽善尽美，要非常专业，不要逃避责任，不要兜圈子，每个功能和测试落到实处。设计要是大牌专业风格。**

### 设计标准
- 布局必须整齐：对齐到网格，间距统一（只用一套 spacing scale），分隔符风格一致
- 比例必须协调：字号层级清晰（不滥用同一字号），视觉重量平衡（左/右、上/下）
- 不留任何视觉瑕疵：没有"差不多"——圆点/竖线/无分隔符混用不可接受
- 大牌风格：克制、专业、不堆砌装饰。内容即设计。参考 Bloomberg Terminal、TradingView、Robinhood 的排版水准

### 实现标准
- 每个 useQuery 的 loading/error/data 三态都必须处理
- 每个 mutation 的 onSuccess/onError/isPending 都必须处理
- 不准有 `catch(_){}` 静默吞错——至少 console.warn
- 不准有未使用的变量/import
- 不准有无依赖数组的 useEffect
- JSX 结构必须正确嵌套——关闭标签不能早关或晚关
- 全屏/弹窗/覆盖层必须验证子元素在正确的容器内

### 测试标准
- 每个组件至少覆盖：正常态 + 加载态 + 错误态 + 空数据态
- 每个交互（点击、切换、展开）必须有测试
- Mock 必须与真实 API 签名一致
- 不准有"只 assert 文本存在"的松散测试——要验证具体值

### 交付标准
- 每次修改后：TypeScript 零错误 + 前端 build 零错误 + 81 测试零失败 + Rust build 零错误
- 构建失败 = 未完成
- 不允许跳过任何一项检查

---

每个问题的工作模式：
1. 启动一个subagent A, 负责具体的任务执行。
2. 启动一个subagent B, 负责问题的分析和策略制定, 监督 A的执行和代码review.
3. 启动3个subagent C,D,E, 负责测试评估任务的执行结果。
4. 主agent负责根据任务和执行方案，制定一个检查表，并监督整个任务的执行过程，确保各个subagent之间的协作顺畅。
5. 任务每一轮结束后，把检查表给用户审查，检查表中包含任务执行的各个环节和评估指标，用户可以根据检查表提出修改意见。根据用户的反馈，主agent会调整任务执行方案，并重新分配subagent的任务，确保最终结果符合用户的期望。